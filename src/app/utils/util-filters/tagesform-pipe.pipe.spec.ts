import {TagesformPipe} from "./tagesform-pipe.pipe";

describe('TagesformPipe', () => {
  it('create an instance', () => {
    const pipe = new TagesformPipe();
    expect(pipe).toBeTruthy();
  });
});
